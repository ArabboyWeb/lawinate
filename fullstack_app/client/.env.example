# Optional: set only if you want frontend to call backend directly.
# Leave this unset on Netlify/custom-domain production so the frontend uses the same-origin /api proxy.
# REACT_APP_API_URL=https://lawinate-sc7t.onrender.com
